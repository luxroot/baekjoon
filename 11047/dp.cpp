#include <iostream>
#include <vector>

using namespace std;

int main()
{
    int n, money, i,j;
    cin >> n;
    vector<int> curr;
    curr.reserve(n);
    cin >> money;
    for(i=0;i<n;i++)
        cin >> curr[i];
    // vector<int> 
    // 
    // This prob can be solved by greedy
    // 
}
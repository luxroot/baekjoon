#include <iostream>
#include <vector>
#include <string>

using namespace std;

int main()
{
    int n,m.i;
    vector<string> first;
    vector<string> last;
    string temp;
    cin >> n >> m;
    for(i=0;i<n;i++)
    {
        cin >> temp;
        first.push_back(temp);
    }
    for(i=0;i<m;i++){
        
    }
}